---
name: "cli"
commands:
  - name: cmd
    commands:
      - name: sub
        arguments:
          - named: true
            short-name: "v"
            completion:
              type: "static"
              values: ["a", "b", "c"]
